<div class="container">
    <div class="card">
    <div class="card-content">
        {{#each policy}}

        <span class="card-title">{{ title }}</span>
    
            <div style="padding-left: 30px">
                <p>{{{ desc }}}</p>
            </div>
       

        {{/each }}  
    </div>
</div>
</div>
